# Ready-to-use Codex task

Implement the requested PDF from `papers/` as an isolated project under `implementations/<paper-title-slug>/`.

Follow the root `AGENTS.md` and every policy in `paper_impl_harness/harness/`. Determine the exact paper title before preparing the workspace and pass it through `paper-harness prepare --title`. Never use `_useless/`, never place paper-specific output at the repository root, and never modify `paper_impl_harness/` while implementing a paper.

Assess feasibility before coding. If permitted, finish the supported implementation, tests, paper summary, implementation explanation, paper-to-code alignment, validation report, and workspace README. Run the final harness gate with validation commands. Clearly distinguish reproduced evidence from assumptions, reduced-scale checks, deviations, and unverified claims.

